function uploadFile (accessToken, filePath) {
  var dbox = require('dbox'),
      sys = require('sys'),
      exec = require('child_process').exec;
  var child;
	var APP_KEY = '7a870efg25be689',
      APP_SECRET = '0ly99wm2hgau5qq';

  var app = dbox.app({
      'app_key': APP_KEY,
      'app_secret': APP_SECRET
  });


  child = exec("python test.py", function (error, stdout, stderr) {
    sys.print('stdout: ' + stdout);
    sys.print('stderr: ' + stderr);
    if (error !== null) {
      console.log('exec error: ' + error);
    }
  });
 //  var client = app.client(accessToken)
 //  // console.log(client);
 //  // return client.cp("a.txt", "b.txt",{"root": "dropbox"}, function (status, reply) {
 //  //   console.log(reply);
 //  // });
 //  return client.put('a.txt', "here is some text", function(status, reply){
	//   console.log(reply);
 //    return client.shares(filePath, {"root": "dropbox"}, function(status, sh_reply){
 //      console.log(sh_reply['url']);
 //      return sh_reply['url'];
 //    });
	// });

}

module.exports = uploadFile;